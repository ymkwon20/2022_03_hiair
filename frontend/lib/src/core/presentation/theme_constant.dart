import 'package:flutter/material.dart';

class ThemeConstant {
  const ThemeConstant._();

  static const MaterialColor dominantColor = Colors.indigo;
  static const complementaryColor = Color(0xFF999999);
  static const accentColor = Color(0XFF8AAAE3);
  static const errorColor = Color(0xFFFF764C);

  static const shades = [
    Color(0xFF4E24E1),
    Color(0xFF7B48FF),
    Color(0xFFA46AFF),
    Color(0xFFCD8DFF),
    Color(0xFFF6B2FF),
  ];
}
